import React from 'react';
import Button from '../../components/ui/button/Button';

const Login = () => {
    return (
        <div>
            <Button>Login</Button>
        </div>
    );
}

export default Login;
