import React from 'react';
import Button from '../../../ui/button/Button';

const Login = () => {
    return (
        <div>
            <Button>Login</Button>
        </div>
    );
}

export default Login;
